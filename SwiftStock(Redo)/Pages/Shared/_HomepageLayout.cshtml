<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SwiftStock</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
    <style>
        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
        }

        .aspect-square {
            aspect-ratio: 1 / 1;
        }
    </style>
</head>
<body class="bg-[#0f1117] text-white min-h-screen flex flex-col">

    <!-- Header/Navbar -->
    <header class="bg-red-600 text-white p-4 shadow-md flex justify-between items-center">
        <div class="text-xl font-bold">SwiftStock</div>

        <div class="flex items-center gap-4">
            <!-- Search -->
            <form method="get" asp-page="/Home">
                <div class="relative">
                    <input type="text" name="search" placeholder="Search..." class="rounded-md px-3 py-1 text-black w-64" />
                    <i class="fas fa-search absolute right-3 top-2 text-gray-400"></i>
                </div>
            </form>

            <!-- Category Dropdown -->
            <select class="rounded-md text-black px-2 py-1">
                <option>All Categories</option>
                <option>Groceries</option>
                <option>Snacks</option>
                <option>Beverages</option>
            </select>

            <a href="/Settings" class="flex items-center gap-2 hover:bg-red-700 px-3 py-2 rounded-md">
                <i class="fas fa-user-circle text-xl"></i>
                <span class="text-sm">Account Settings</span>
            </a>

        </div>
    </header>

    <!-- Navigation -->
    <nav class="bg-[#0f1117] border-b border-slate-800 p-3 flex gap-4 justify-center text-white">
        <a href="/" class="px-4 py-2 hover:text-red-400">Home</a>
        <a href="/Shop" class="px-4 py-2 hover:text-red-400">Shop</a>
        <a href="/About" class="px-4 py-2 hover:text-red-400">About Us</a>
    </nav>

    <!-- Main Content -->
    <main class="flex-1 p-6 container mx-auto">
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="bg-red-600 text-white text-center py-4">
        &copy; 2025 SwiftStock
    </footer>

    @RenderSection("Scripts", required: false)

</body>
</html>
